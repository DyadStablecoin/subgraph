type DNft @entity {
  id: ID!
  xp: Int!
  deposit: Int!
  withdrawal: Int!
  lastOwnershipChange: Int!
  isActive: Boolean!
  owner: User!
}

type User @entity {
  id: ID!
  tokens: [DNft!]! @derivedFrom(field: "owner")
}
